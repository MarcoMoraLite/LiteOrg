<?xml version="1.0" encoding="utf-8"?>
<odoo>

   <data noupdate="1">

       <!--Email template -->

       <record model="mail.template" id="template_contrato">
           <field name="name">Template de contratos</field>
           <field name="model_id" ref="valida_curp.valida_curp.valida_curp"/>
           <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
           <!--field name="email_to" >${object.partner_id.email}</field-->
           <field name="subject">Stock faltante Venta Ref ${object.name or 'n/a' }</field>
           <field name="auto_delete" eval="True"/>
           <field name="lang">${object.partner_id.lang}</field>
           <field name="body_html"><![CDATA[
                Alerta automática por falta de stock:<br><br>
                
                 ]]>

           </field>

       </record>

   </data>

</odoo>